// Placeholder — the admin "Categories" screen is built separately.
export default function AdminCategoriesPage() {
  return (
    <main className="grid min-h-dvh place-items-center p-8 text-center">
      <div>
        <p className="text-xs font-semibold uppercase tracking-[0.2em] text-zinc-400">
          Admin
        </p>
        <h1 className="mt-2 text-2xl font-semibold">Category management</h1>
        <p className="mt-2 text-sm text-zinc-500">Management UI coming soon.</p>
      </div>
    </main>
  );
}
